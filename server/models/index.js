const User = require('./User');
const Swimmer = require('./Swimmer');
const Post = require('./Post');
const Meet = require('./Meet');
const Entries = require('./Entries');

module.exports = { User, Swimmer, Post, Meet, Entries};
